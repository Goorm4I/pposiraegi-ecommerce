spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_test_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop # 테스트 시작 시 생성, 종료 시 삭제
    show-sql: true